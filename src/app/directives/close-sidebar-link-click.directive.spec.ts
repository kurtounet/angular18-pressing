import { CloseSidebarLinkClickDirective } from './close-sidebar-link-click.directive';

describe('CloseSidebarLinkClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseSidebarLinkClickDirective();
    expect(directive).toBeTruthy();
  });
});
