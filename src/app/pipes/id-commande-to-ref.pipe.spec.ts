import { IdCommandeToRefPipe } from './id-commande-to-ref.pipe';

describe('IdCommandeToRefPipe', () => {
  it('create an instance', () => {
    const pipe = new IdCommandeToRefPipe();
    expect(pipe).toBeTruthy();
  });
});
