import { NameServiceByIdPipe } from './name-service-by-id.pipe';

describe('NameServiceByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new NameServiceByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
